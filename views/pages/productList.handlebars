<div class="row div-width-95">
    <div class="col-md-11">
        <h3 class="title">{{title}}</h3>
    </div>
    <div class="col-md-1">
        <a class="btn moreItems" href="/channel?category={{category}}">more</a>
    </div>
    {{#each products}} {{> products this}} {{/each}}
</div>
